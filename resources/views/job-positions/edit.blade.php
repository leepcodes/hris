<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">Edit Position</h2></x-slot><div class="max-w-2xl mx-auto px-4 py-6"><form method="POST" action="{{ route('job-positions.update',$jobPosition) }}" class="space-y-4 bg-white p-6 border rounded">@csrf @method('PUT')<select name="department_id" class="w-full border rounded p-2">@foreach($departments as $department)<option value="{{ $department->id }}" @selected($jobPosition->department_id===$department->id)>{{ $department->name }}</option>@endforeach</select><input name="title" class="w-full border rounded p-2" value="{{ old('title',$jobPosition->title) }}"><input name="salary_grade" class="w-full border rounded p-2" value="{{ old('salary_grade',$jobPosition->salary_grade) }}"><input name="basic_salary" type="number" step="0.01" class="w-full border rounded p-2" value="{{ old('basic_salary',$jobPosition->basic_salary) }}"><input name="daily_rate" type="number" step="0.01" class="w-full border rounded p-2" value="{{ old('daily_rate',$jobPosition->daily_rate) }}"><input name="hourly_rate" type="number" step="0.01" class="w-full border rounded p-2" value="{{ old('hourly_rate',$jobPosition->hourly_rate) }}"><button class="px-3 py-2 bg-slate-900 text-white rounded">Update</button></form></div></x-app-layout>
