<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">System Configuration</h2></x-slot><div class="max-w-4xl mx-auto px-4 py-6 space-y-4"><form method="POST" action="{{ route('system-config.store') }}" class="bg-white border rounded p-4 grid grid-cols-1 gap-3">@csrf<input name="group" class="border rounded p-2" placeholder="Group"><input name="key" class="border rounded p-2" placeholder="Key"><input name="value" class="border rounded p-2" placeholder="Value"><button class="px-3 py-2 bg-slate-900 text-white rounded">Save</button></form><div class="bg-white border rounded p-4">@foreach($grouped as $group => $items)<h3 class="font-semibold mt-3">{{ $group }}</h3>@foreach($items as $item)<p class="text-sm">{{ $item->key }}: {{ $item->value['value'] ?? '' }}</p>@endforeach @endforeach</div></div></x-app-layout>
