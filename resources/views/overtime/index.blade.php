<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">Overtime Management</h2></x-slot><div class="max-w-7xl mx-auto px-4 py-6 space-y-4"><form method="POST" action="{{ route('overtime.store') }}" class="bg-white border rounded p-4 grid grid-cols-2 gap-3">@csrf<label class="block text-sm font-medium">Employee</label><select name="employee_id" class="border rounded p-2">@foreach($employees as $e)<option value="{{ $e->id }}">{{ $e->last_name }}, {{ $e->first_name }}</option>@endforeach</select><label class="block text-sm font-medium">OT Date</label><input name="ot_date" type="date" class="border rounded p-2"><label class="block text-sm font-medium">Start Time</label><input name="start_time" type="time" class="border rounded p-2"><label class="block text-sm font-medium">End Time</label><input name="end_time" type="time" class="border rounded p-2"><label class="block text-sm font-medium col-span-2">Reason</label><textarea name="reason" class="border rounded p-2 col-span-2"></textarea><button class="px-3 py-2 bg-slate-900 text-white rounded col-span-2">File Overtime</button></form><div class="bg-white border rounded overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="border-b"><th class="p-2">Employee</th><th>Date</th><th>Hours</th><th>Status</th><th></th></tr></thead><tbody>@foreach($overtimeRequests as $ot)<tr class="border-b"><td class="p-2">{{ $ot->employee_id }}</td><td>{{ $ot->ot_date }}</td><td>{{ $ot->hours }}</td><td>{{ $ot->status }}</td><td class="flex gap-2"><form method="POST" action="{{ route('overtime.update',$ot) }}">@csrf @method('PUT')<input type="hidden" name="ot_date" value="{{ $ot->ot_date }}"><input type="hidden" name="start_time" value="{{ substr((string)$ot->start_time,0,5) }}"><input type="hidden" name="end_time" value="{{ substr((string)$ot->end_time,0,5) }}"><input type="hidden" name="reason" value="{{ $ot->reason }}"><button class="text-blue-700">Edit Save</button></form><form method="POST" action="{{ route('overtime.destroy',$ot) }}">@csrf @method('DELETE')<button class="text-red-700">Delete</button></form>@if($ot->status==='pending')<form method="POST" action="{{ route('overtime.approve',$ot) }}">@csrf<button class="text-emerald-700">Approve</button></form><form method="POST" action="{{ route('overtime.reject',$ot) }}">@csrf<button class="text-amber-700">Reject</button></form>@endif</td></tr>@endforeach</tbody></table></div>{{ $overtimeRequests->links() }}</div></x-app-layout>
