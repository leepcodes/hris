<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">Payroll Periods</h2></x-slot><div class="max-w-7xl mx-auto px-4 py-6 space-y-4"><a href="{{ route('payroll-periods.create') }}" class="px-3 py-2 bg-slate-900 text-white rounded">New Period</a><div class="bg-white border rounded overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="border-b"><th class="p-2">Name</th><th>Frequency</th><th>Dates</th><th>Status</th><th></th></tr></thead><tbody>@foreach($periods as $period)<tr class="border-b"><td class="p-2">{{ $period->name }}</td><td>{{ $period->frequency }}</td><td>{{ $period->start_date }} - {{ $period->end_date }}</td><td>{{ $period->status }}</td><td><a class="text-blue-700" href="{{ route('payroll-periods.edit',$period) }}">Edit</a></td></tr>@endforeach</tbody></table></div>{{ $periods->links() }}</div></x-app-layout>
