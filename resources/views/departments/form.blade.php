<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">Department Form</h2></x-slot><div class="max-w-xl mx-auto px-4 py-6"><form method="POST" action="{{ isset($department) ? route('departments.update',$department) : route('departments.store') }}" class="space-y-4 bg-white p-6 border rounded">@csrf @isset($department) @method('PUT') @endisset<input name="code" placeholder="Code" class="w-full border rounded p-2" value="{{ old('code',$department->code ?? '') }}"><input name="name" placeholder="Name" class="w-full border rounded p-2" value="{{ old('name',$department->name ?? '') }}"><label class="flex items-center gap-2"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$department->is_active ?? true))>Active</label><button class="px-3 py-2 bg-slate-900 text-white rounded">Save</button></form></div></x-app-layout>
