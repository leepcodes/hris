<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-slate-800">Employee Self-Service</h2></x-slot><div class="max-w-7xl mx-auto px-4 py-6 space-y-6"><div class="bg-white border rounded p-4"><p class="font-semibold">{{ $employee?->last_name }}, {{ $employee?->first_name }}</p><p class="text-sm text-slate-600">{{ $employee?->email }}</p></div><div class="grid grid-cols-1 md:grid-cols-2 gap-4"><div class="bg-white border rounded p-4"><h3 class="font-semibold mb-2">Leave Requests</h3>@foreach($leaveRequests as $r)<p class="text-sm">{{ $r->start_date }} - {{ $r->end_date }} ({{ $r->status }})</p>@endforeach</div><div class="bg-white border rounded p-4"><h3 class="font-semibold mb-2">Leave Credits</h3>@foreach($leaveCredits as $c)<p class="text-sm">Type {{ $c->leave_type_id }}: {{ $c->balance }}</p>@endforeach</div><div class="bg-white border rounded p-4"><h3 class="font-semibold mb-2">Overtime</h3>@foreach($overtimeRequests as $r)<p class="text-sm">{{ $r->ot_date }} {{ $r->hours }}h ({{ $r->status }})</p>@endforeach</div><div class="bg-white border rounded p-4"><h3 class="font-semibold mb-2">Loans</h3>@foreach($loans as $l)<p class="text-sm">Loan {{ $l->id }} balance {{ $l->balance_amount }} ({{ $l->status }})</p>@endforeach</div></div></div></x-app-layout>
